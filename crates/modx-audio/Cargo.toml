[package]
name = "modx-audio"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Default features, so the host stays WASAPI in shared mode: the same one the
# fase 0 numbers were measured on (441 frames per callback, constant). The `asio`
# feature is deliberately not enabled — it needs the Steinberg SDK downloaded by
# hand and would change every number in `docs/results`.
cpal = "0.15.3"
thiserror.workspace = true
