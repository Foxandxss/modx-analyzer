[package]
name = "modx-midi"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Default features, so the Windows backend stays WinMM — the one the fase 0c
# latency and loss numbers were measured on.
midir = "0.11"
thiserror.workspace = true
