design_handoff/
ui/dist/
ui/.angular/
target/
src-tauri/target/
src-tauri/gen/
src-tauri/icons/
pnpm-lock.yaml

# Kept verbatim from the design package: it is the source of truth for values,
# and a reflow would make the diff against the handoff unreadable.
ui/src/styles/design-tokens.css

# Prose in Spanish, hand-wrapped. Prettier would reflow it.
**/*.md

# Generado por `every_topology()` en src-tauri: es un volcado de la tabla de los
# 88, no código, y el test `the_harness_table_is_this_table` compara byte a byte
# con lo que Rust escribe. Reformatearlo aquí lo pondría en rojo allí.
ui/src/app/dev/algorithms.json
